name: ci
on:
  push: { branches: [main, develop, "feature/**", "release/**"] }
  pull_request: { branches: [main, develop] }
jobs:
  frontend:
    runs-on: ubuntu-latest
    defaults: { run: { working-directory: frontend } }
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: npm ci
      - run: npx tsc -b
      - run: npx vitest run
      - run: npm run build
  server:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - run: pip install -e "${{ vars.VW_ENGINE_PATH || './engine' }}" -c constraints.txt
      - run: pip install fastapi "uvicorn[standard]" httpx pytest anthropic jsonschema pandas -c constraints.txt
      - run: cd engine && python -m pytest tests -q
      - run: python -m pytest server/tests -q
  contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - run: pip install -e ./engine -c constraints.txt
      - run: pip install jsonschema pandas -c constraints.txt
      - run: python scripts/verify_contracts.py
      - run: python scripts/verify_prompts.py
      - run: python scripts/run_eval.py
