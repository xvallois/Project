# ADR 0006 — One blotter
**Status** locked by product owner (pre-Phase 0) · **Decision** a single
ledger table, `kind: paper|live|idea|invalidated`,
`linked_opportunity_id`, thesis frozen at entry WITH provenance refs,
outcome required on close.
**Why** the value is the decision lifecycle, not execution type; this is
the dataset for attribution and Analyst learning.
